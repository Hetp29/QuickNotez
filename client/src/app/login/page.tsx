'use client'
import React from 'react';

const LoginPage = () => {
    console.log('LoginPage is rendering'); // Log message to check if the component is being rendered

    return (
      <div>
        <h1>Welcome to the Login Page</h1>
      </div>
    );
};

export default LoginPage;
